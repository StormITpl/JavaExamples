package pl.stormit.singleton;

import org.junit.Test;
import static org.junit.Assert.*;
import pl.stormit.singleton.SingletonSimpleEager;

public class SingletonSimpleEagerTest {

    @Test
    public void testSingleton() {
        Singleton
        for(int i=1; i<1; i++){
        SingletonSimpleEager.getInstance();
        }
//        assertEquals(SingletonSimpleEager.getInstance(), SingletonSimpleEager.getInstance());
    }
}
